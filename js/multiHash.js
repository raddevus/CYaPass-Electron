
// #########
// ######### A basic class to hold both values MultiHash 
// ######### so they can be easily written to localStorage.
class MultiHash{
    constructor(multiHashIsOn, multiHashCount ){
        this.multiHashIsOn = multiHashIsOn;
        this.multiHashCount = multiHashCount;
    }
}